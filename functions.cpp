#include "functions.hpp"

#define N 100
#define M 100

namespace func{
    /*int MinPrime(int& n, int& m, int matrix[N][M]){
        for(int i = 0; i < n; i++){
            for(int j = 0; j < m; j++){
                if (matrix[i][j] >= 2){
                    for (int d = 2; d < matrix[i][j]; d++){
                        if(matrix[i][j] % d == 0)
                            checkPr = false;
                        if(checkPr){
                            if(matrix[i][j] < minPrime)
                                minPrime = matrix[i][j];
                        }
                            
                    }
                }
            }
        }
    }
    void Read(int& n, int& m, int mas[N][M]){
        for(int i = 0; i < n; i++){
            for(int j = 0; j < m; j++){
                in >> matrix[i][j];
            }
        }
    }*/

}

